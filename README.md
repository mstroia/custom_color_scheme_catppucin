# Gruvbox themed remix of catppuccin Stylus base color scheme

This repo contains a .less file that modifies the default less file for the catppuccin Stylus extension themes to use gruvbox colors instead.